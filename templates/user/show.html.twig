{% extends '@AcMarcheApi/base.html.twig' %}

{% block title %}{{ user }}{% endblock %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header">
            <h2>{{ user }}</h2>
        </div>
        <div class="card-body">
            {% include '@AcMarcheApi/user/_actions.html.twig' %}

        </div>
    </div>

    {{ include('@AcMarcheApi/user/_delete_form.html.twig') }}
{% endblock %}
